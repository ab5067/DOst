package com.example.dost.ServerClientNetwork;

import java.io.IOException;

public class Afflicted extends client{

    public Afflicted(String hostname, int port, String username, String type) throws IOException {
        super(hostname, port, username, type);
    }
}
